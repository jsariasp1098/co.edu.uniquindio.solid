package co.edu.unquindio.solid.model;

public class guardarUsuario extends Usuario{
    public guardarUsuario() {
    }
    public guardarUsuario(String nombre, String ID, String email, String celular, String direccion) {
        super(nombre, ID, email, celular, direccion);
    }
}
